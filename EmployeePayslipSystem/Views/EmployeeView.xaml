<UserControl x:Class="EmployeePayslipSystem.Views.EmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border BorderBrush="Gray" BorderThickness="1" Padding="15">
            <StackPanel>
                <TextBlock Text="Employee Details"
                           FontSize="16"
                           FontWeight="Bold"
                           Margin="0,0,0,15"/>

                <TextBlock Text="Employee ID"/>
                <TextBox Text="{Binding Employee.EmployeeId, UpdateSourceTrigger=PropertyChanged}" 
                         IsReadOnly="{Binding IsEditMode}"
                         Margin="0,5,0,10">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                    <Setter Property="Background" Value="LightGray"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <TextBlock Text="Employee Name"/>
                <TextBox Text="{Binding Employee.EmployeeName, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,5,0,10"/>

                <TextBlock Text="Designation"/>
                <TextBox Text="{Binding Employee.Designation, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,5,0,10"/>

                <TextBlock Text="Joining Date"/>
                <DatePicker SelectedDate="{Binding Employee.JoiningDate}" 
                            Margin="0,5,0,10"/>

                <TextBlock Text="Basic Salary (₹)"/>
                <TextBox Text="{Binding Employee.BasicSalary}" 
                         Margin="0,5,0,10"/>

                <TextBlock Text="HRA (%)"/>
                <TextBox Text="{Binding Employee.HRA_Percent}" 
                         Margin="0,5,0,10"/>

                <TextBlock Text="DA (%)"/>
                <TextBox Text="{Binding Employee.DA_Percent}" 
                         Margin="0,5,0,10"/>

                <TextBlock Text="Other Allowance (%)"/>
                <TextBox Text="{Binding Employee.OtherAllowance_Percent}" 
                         Margin="0,5,0,10"/>

                <TextBlock Text="PF (%)"/>
                <TextBox Text="{Binding Employee.PF_Percent}" 
                         Margin="0,5,0,10"/>

                <TextBlock Text="ESI (%)"/>
                <TextBox Text="{Binding Employee.ESI_Percent}" 
                         Margin="0,5,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="{Binding SaveButtonText}"
                            Command="{Binding SaveCommand}"
                            Background="DarkGreen"
                            Foreground="White"
                            Width="150"
                            Height="35"
                            FontWeight="Bold"
                            Margin="0,0,10,0"/>

                    <Button Content="Cancel"
                            Command="{Binding CancelCommand}"
                            Background="Gray"
                            Foreground="White"
                            Width="80"
                            Height="35"
                            FontWeight="Bold"
                            Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="10,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                           Text="Employee List" 
                           FontSize="16" 
                           FontWeight="Bold" 
                           Margin="0,0,0,10"/>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Employees}"
                          SelectedItem="{Binding SelectedEmployee}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Employee ID" Binding="{Binding EmployeeId}" Width="100"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding EmployeeName}" Width="150"/>
                        <DataGridTextColumn Header="Designation" Binding="{Binding Designation}" Width="120"/>
                        <DataGridTextColumn Header="Joining Date" Binding="{Binding JoiningDate, StringFormat=dd-MMM-yyyy}" Width="120"/>
                        <DataGridTextColumn Header="Basic Salary" Binding="{Binding BasicSalary, StringFormat=₹{0:N2}}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="2" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right" 
                            Margin="0,10,0,0">
                    <Button Content="Edit Selected"
                            Command="{Binding EditCommand}"
                            Background="DarkBlue"
                            Foreground="White"
                            Width="120"
                            Height="35"
                            FontWeight="Bold"
                            Margin="0,0,10,0"/>

                    <Button Content="Delete Selected"
                            Command="{Binding DeleteCommand}"
                            Background="DarkRed"
                            Foreground="White"
                            Width="120"
                            Height="35"
                            FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>